v1.12
- Fixed encrypted backup export writing a 0-byte file after returning from the system file picker
- Added support for importing your own encrypted backups (auto-detected, with a backup-password prompt to decrypt)
- Lock screen changed to an overlay to avoid losing the result callback during SAF round-trips
